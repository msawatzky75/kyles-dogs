<%= form_with(model: invoice_status_code, local: true) do |form| %>
  <% if invoice_status_code.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(invoice_status_code.errors.count, "error") %> prohibited this invoice_status_code from being saved:</h2>

      <ul>
      <% invoice_status_code.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
